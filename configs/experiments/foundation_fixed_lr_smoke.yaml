# Full-data smoke via the production runner; do not pass its reduced --smoke flag.
presets:
  - foundation_fixed_lr_20epochs.yaml
experiment:
  name: foundation_fixed_lr_smoke
  output_root: runs/pbt
shared:
  generations: 1
