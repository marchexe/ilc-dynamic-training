schema_version: 1
presets:
  - ../presets/shared/pretrained_epoch17_ranger_parquet.yaml
  - ../presets/resources/local_4gpu.yaml
  - ../presets/population/members_4.yaml
  - ../presets/pbt/guarded_smooth_lr_mistag.yaml

experiment:
  output_root: runs/pbt
  name: pretrained_guarded_4gpu_smooth_lr

shared:
  generations: 8
  samples_per_epoch: 480000

pbt:
  early_stop_degraded_generations: 4
