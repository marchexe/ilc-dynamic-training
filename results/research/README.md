# Research Results

This directory is for lightweight, Git-trackable summaries exported from local training runs.
Keep heavy artifacts such as `.pt`, logs, plots, datasets, and full `runs/` directories out of Git.

Use `scripts/reports/export_research_result.py` to export compact JSON/CSV files containing run metrics, baseline comparison, seeds, strategy settings, and aggregate mean/std values.
